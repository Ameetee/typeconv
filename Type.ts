// code1 conversion
// TypeScript Conversion Steps
// Define Props Type: Create an interface to define the shape of the props object.

// Annotate Function Component: Use TypeScript’s React.FC type (React Functional Component) and pass the props type.


// import React from 'react';

// // Step 1: Define the type for props
// interface GreetingProps {
//   name: string;
// }

// // Step 2: Use React.FC and annotate with GreetingProps
// const Greeting: React.FC<GreetingProps> = ({ name }) => {
//   return <div>Hello, {name}!</div>;
// };

// export default Greeting;
