// TypeScript Conversion Steps code2
// Define State Type: Create an interface for the state.

// Define Props Type: If your component uses props, define the type for props. In this case, no props are used.

// Annotate Component Class: Use TypeScript’s type annotations for the state and methods.

// import { Button } from 'bootstrap';
// import React, { Component } from 'react';

// // Step 1: Define the type for state
// interface CounterState {
//   count: number;
// }

// // // Step 2: Annotate the component with the state type
// // class Counter extends Component<{}, CounterState> {
// //   // Initialize state with type
// //   state: CounterState = {
// //     count: 0
// //   };

// //   // Method to update state
// //   increment = () => {
// //     this.setState(prevState => ({ count: prevState.count + 1 }));
// //   };

// //   // Render method
// //   render() {
// //     return (
// //       <div>
// //         <p>Count: {this.state.count}</p>
// //         <Button onclick={this.increment}>Increment </Button>
// //       </div>
//     );
//   }
// }

export default Counter;
